export default function ({ Presenter }) {
    class TicketBatchPresenterClass extends Presenter {
        static type = 'ticketBatch';
        static plural = 'ticketBatches';
    }
    return TicketBatchPresenterClass;
}
